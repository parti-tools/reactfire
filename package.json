{
  "private": true,
  "devDependencies": {
    "flow-mono-cli": "^1.3.0"
  },
  "scripts": {
    "link": "flow-mono create-symlinks ./build/.flowconfig",
    "prepare": "yarn run link",
    "compile:all": "lerna exec --ignore dev-scripts --ignore example -- yarn compile",
    "compile:watch:all": "node ./packages/dev-scripts/watch.js",
    "bootstrap": "lerna bootstrap"
  },
  "workspaces": ["packages/*"]
}
